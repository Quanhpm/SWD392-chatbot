import React, { useState } from 'react';
import { useApp } from '../context/AppContext.js';
import { useAuth } from '../context/AuthContext.js';
import { deleteDocument, retryDocument, updateDocument } from '../services/documentApi.js';
import type { IDocument } from '../types/index.js';

const ownerId = (document: IDocument): string | undefined => typeof document.uploadedBy === 'string' ? document.uploadedBy : document.uploadedBy?._id ?? document.uploadedBy?.id;
const statusLabel = { uploaded: 'Đã tải lên', processing: 'Đang xử lý', ready: 'Sẵn sàng', failed: 'Xử lý lỗi' } as const;

export const TeacherDashboard: React.FC = () => {
  const { state, dispatch, refreshDocuments } = useApp();
  const { state: auth } = useAuth();
  const [message, setMessage] = useState<string | null>(null);
  const myId = auth.user?._id ?? auth.user?.id;
  const myDocuments = state.documents.filter((doc) => ownerId(doc) === myId);
  const run = async (action: () => Promise<void>, success: string) => { try { await action(); setMessage(success); await refreshDocuments(); } catch (error) { setMessage(error instanceof Error ? error.message : 'Thao tác thất bại.'); } };
  const edit = async (document: IDocument) => {
    const originalName = prompt('Tên hiển thị tài liệu', document.originalName); if (!originalName) return;
    const chapterTitle = prompt('Tên chương', document.chapterTitle); if (!chapterTitle) return;
    const chapter = Number(prompt('Số chương', String(document.chapter))); if (!Number.isInteger(chapter) || chapter < 0) return;
    await run(async () => { const updated = await updateDocument(document._id, { originalName, chapter, chapterTitle }); dispatch({ type: 'UPDATE_DOCUMENT', payload: updated }); }, 'Đã cập nhật metadata.');
  };

  return <div className="teacher-page">
    <header><div><span className="page-eyebrow"><span className="material-symbols-outlined">school</span>Teacher workspace</span><h1>Quản lý nội dung môn học</h1><p>Bạn chỉ có thể upload và quản lý tài liệu của mình trong môn đang được phân công.</p></div><button className="btn-primary" onClick={() => dispatch({ type: 'SET_UPLOAD_MODAL', payload: true })}><span className="material-symbols-outlined">upload_file</span>Upload tài liệu</button></header>
    {message && <div className="teacher-message">{message}</div>}
    <section className="teacher-metrics"><article><div className="metric-icon indigo"><span className="material-symbols-outlined">class</span></div><div><strong>{state.subjects.length}</strong><small>Môn được phân công</small></div></article><article><div className="metric-icon blue"><span className="material-symbols-outlined">description</span></div><div><strong>{myDocuments.length}</strong><small>Tài liệu của tôi</small></div></article><article><div className="metric-icon teal"><span className="material-symbols-outlined">task_alt</span></div><div><strong>{myDocuments.filter((doc) => doc.status === 'ready').length}</strong><small>Đã sẵn sàng</small></div></article><article><div className="metric-icon rose"><span className="material-symbols-outlined">error</span></div><div><strong>{myDocuments.filter((doc) => doc.status === 'failed').length}</strong><small>Cần xử lý</small></div></article></section>
    <section className="teacher-card"><div className="section-title"><div><h2>Môn học được phân công</h2><p>Admin có thể gán nhiều giảng viên cho cùng một môn.</p></div></div><div className="subject-pills">{state.subjects.map((subject) => <span key={subject._id}><strong>{subject.code}</strong>{subject.name}</span>)}{state.subjects.length === 0 && <p>Chưa được phân công môn học.</p>}</div></section>
    <section className="teacher-card"><div className="section-title"><div><h2>Tài liệu của tôi</h2><p>Tài liệu tự công bố sau khi xử lý thành công.</p></div><button onClick={() => void refreshDocuments()}>Làm mới</button></div><div className="teacher-table"><table><thead><tr><th>Tài liệu</th><th>Môn</th><th>Trạng thái</th><th>Chunks</th><th>Thao tác</th></tr></thead><tbody>{myDocuments.map((document) => <tr key={document._id}><td><strong>{document.originalName}</strong><small>Chương {document.chapter}: {document.chapterTitle}</small>{document.errorMessage && <em>{document.errorMessage}</em>}</td><td>{document.subject}</td><td><span className={`state ${document.status}`}>{statusLabel[document.status]}</span></td><td>{document.totalChunks}</td><td><div className="row-actions"><button onClick={() => void edit(document)}>Sửa</button>{document.status === 'failed' && <button onClick={() => void run(() => retryDocument(document._id), 'Đã chạy lại xử lý.')}>Retry</button>}<button className="danger" disabled={document.status === 'processing'} onClick={() => { if (confirm('Xóa tài liệu và toàn bộ lịch sử chat liên quan?')) void run(() => deleteDocument(document._id).then(() => undefined), 'Đã xóa tài liệu.'); }}>Xóa</button></div></td></tr>)}</tbody></table>{myDocuments.length === 0 && <div className="empty">Bạn chưa upload tài liệu.</div>}</div></section>
    <style>{`.teacher-page{width:100%;max-width:1280px;margin:auto;padding:36px 40px 48px}.teacher-page header{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;padding:0 0 26px;border-bottom:1px solid var(--color-outline-variant)}.teacher-page h1{font-size:32px;letter-spacing:-.04em;margin:10px 0 8px}.teacher-page header p,.section-title p{color:#64748b;line-height:1.6}.teacher-page header .btn-primary{min-height:44px;padding:10px 16px;border-radius:12px;box-shadow:0 8px 16px rgb(79 70 229 / .16)}.teacher-message{margin:18px 0;padding:11px 14px;background:#eef2ff;color:#3730a3;border:1px solid #c7d2fe;border-radius:11px}.teacher-metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:24px 0}.teacher-metrics article{display:flex;align-items:center;gap:12px;background:#fff;border:1px solid var(--color-outline-variant);border-radius:16px;padding:17px 18px;box-shadow:var(--shadow-sm)}.metric-icon{display:grid;width:40px;height:40px;place-items:center;border-radius:12px}.metric-icon.indigo{background:#eef2ff;color:#4f46e5}.metric-icon.blue{background:#eff6ff;color:#2563eb}.metric-icon.teal{background:#ecfdf5;color:#0f766e}.metric-icon.rose{background:#fff1f2;color:#e11d48}.teacher-metrics strong,.teacher-metrics small{display:block}.teacher-metrics strong{font-size:26px;letter-spacing:-.03em}.teacher-metrics small{margin-top:3px;color:#64748b;font-size:12px}.teacher-card{margin-top:16px;background:#fff;border:1px solid var(--color-outline-variant);border-radius:16px;padding:22px;box-shadow:var(--shadow-sm)}.section-title{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:18px}.section-title h2{font-size:18px;letter-spacing:-.02em}.section-title button,.row-actions button{border:1px solid #dbe1e8;background:white;border-radius:9px;padding:7px 10px;color:#475569}.section-title button:hover,.row-actions button:hover{border-color:#a5b4fc;color:#4338ca;background:#f8faff}.subject-pills{display:flex;gap:9px;flex-wrap:wrap}.subject-pills span{display:flex;gap:8px;padding:10px 12px;border:1px solid #e2e8f0;border-radius:10px;color:#475569;background:#fafbff}.subject-pills strong{color:#4338ca}.teacher-table{overflow:auto}.teacher-table table{width:100%;border-collapse:collapse}.teacher-table th,.teacher-table td{text-align:left;padding:13px 12px;border-bottom:1px solid #eef2f7}.teacher-table th{background:#fafbff}.teacher-table td small,.teacher-table td em{display:block;color:#64748b;margin-top:4px}.teacher-table td em{color:#b91c1c}.state{font-size:11px;font-weight:700;padding:5px 9px;border-radius:20px;background:#f1f5f9}.state.ready{background:#ecfdf5;color:#047857}.state.failed{background:#fef2f2;color:#b91c1c}.state.processing{background:#eff6ff;color:#2563eb}.row-actions{display:flex;gap:5px;white-space:nowrap}.row-actions .danger{color:#b91c1c}.empty{padding:42px;text-align:center;color:#64748b}@media(max-width:900px){.teacher-page{padding:28px}.teacher-metrics{grid-template-columns:1fr 1fr}}@media(max-width:560px){.teacher-page{padding:20px 18px 34px}.teacher-page header{align-items:flex-start;flex-direction:column}.teacher-metrics{grid-template-columns:1fr}.teacher-card{padding:16px}}`}</style>
  </div>;
};
